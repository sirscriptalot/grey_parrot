require "rspec/core/rake_task"

# Instead of writing our own, use RSpec's built-in rake task as "spec".
RSpec::Core::RakeTask.new(:spec)

# When no task argument is given to rake, execute the spec task.
task default: [:spec]
